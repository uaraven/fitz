[package]
name = "fitz"
version = "0.1.0"
edition = "2024"

[dependencies]
fitskit = { version = "0.2.0", features = ["gzip"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1"

[dev-dependencies]
tempfile = "3"
