// Small shared UI atoms used across the app chrome.

// A thin vertical divider between toolbar / status-bar groups.
export component Separator inherits Rectangle {
    width: 1px;
    background: #444444;
}
